# Backend - Customer Spending Insights (Mock API)

Run:
- `npm install`
- `npm run seed` (optional; generates seed-data.json)
- `npm run dev` to start in dev mode (ts-node-dev)

Or build:
- `npm run build`
- `npm start`

API: http://localhost:4000/api/customers/cust_001/summary
